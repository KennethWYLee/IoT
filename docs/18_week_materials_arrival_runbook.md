# 18 週材料與課堂執行 Runbook

本文件處理學生材料尚未到齊、只有教師三套器材、器材故障與第 9 週教師
出國時的課堂備援。它不假設學生製作車輛。

## 1. 教師三套器材的用途

| 用途 | 原則 |
|---|---|
| 課前驗證 | 教師先驗證當週接線、library、程式、供電及預期錯誤 |
| 課堂示範 | 使用一套完成共同示範，避免同時拆動三套已知可用配置 |
| 短期借用 | 有明確任務、借還日期與狀態紀錄，不當作學期固定配發 |
| 故障替換 | 區分板卡、模組、線材或程式問題後才交換零件 |
| 教師作品 | 可供智慧停車、互動玩具或移動平台案例，不影響學生選題 |

## 2. 到貨優先順序

| 最晚週次 | 每組需要 | 未到貨時的等價活動 |
|---:|---|---|
| 1 | 筆電、Arduino IDE、USB 資料線；開發板可共用驗證 | 閱讀 upload／Serial 錯誤，完成環境與資料流分析 |
| 2 | ESP32-S3、麵包板、杜邦線、按鈕、LED／RGB、蜂鳴器、電阻 | 使用教師 rig 輪流測試；其他人先做接線安全與萬用電表任務 |
| 3 | 任兩種感測器或輸入 | 使用同一感測器改測試條件；教師提供錄製 raw data |
| 4 | 至少一種致動／顯示元件與適當電源 | 先用 LED／蜂鳴器完成狀態與 timeout，不因沒有舵機停課 |
| 5 | Week 2-4 元件重組 | 使用教師套件分時完成三次單機測試 |
| 6 | 可連 Wi-Fi 的 ESP32、學生筆電 | 以手機熱點或教師測試網路；ESP32 缺席組先用 HTTP client 模擬 |
| 8 | 題目所需特殊材料只需列計畫，不要求全數到貨 | 紙模型、單一硬體片段與軟體 mock，但明確標示真假 |
| 13 | 期末真實核心硬體 | 未到貨功能刪減；不能用全程 mock 通過進度檢查 |

## 3. 每組基本材料建議

- ESP32-S3 DevKitC-1 或經核准同等板卡、USB 資料線。
- 麵包板、公對公／公對母／母對母杜邦線、常用電阻。
- 按鈕、LED／RGB 或有源蜂鳴器。
- DHT11、PIR、光敏、距離或其他至少一種輸入。
- SG90、WS2812、OLED 或其他至少一種輸出；舵機須有適當外部供電。
- 收納盒、標籤與每組可借用的萬用電表。

題目確認前不要求購買感測器大全包、相機板、Raspberry Pi、車體、LiDAR
或高價 AI 板。特殊材料在第 8 週確認用途、安全與預算後再買。

## 4. 每週器材準備

| 週次 | 教師課前驗證 | 教室備品 |
|---:|---|---|
| 1 | Arduino-ESP32、board／port、Serial | 已知良好 USB 資料線與一片可上傳板 |
| 2 | GPIO、按鈕、LED／蜂鳴器、萬用電表 | 電阻、線材、備用按鈕與麵包板 |
| 3 | 各感測器供電、邏輯準位、library 與有效範圍 | 至少兩種已驗證感測器與 raw data |
| 4 | 舵機／LED 供電、共地、停止與 timeout | 外部 5V 電源、備用輸出與斷電方式 |
| 5 | 可重複三次的單機作品與故障例 | Week 2-4 元件、計時器、標籤 |
| 6 | 測試網路、HTTP backend、WebSocket 手機頁 | 備援熱點、已知可用 server 與錯誤 log |
| 7 | 試卷與答案分離；command／result 範例 | 紙本備援、借用板與已驗證控制負載 |
| 8 | 預約表、訪談表與材料相容性檢查資料 | 教師三套借用材料與紙模型材料 |
| 10-12 | MQTT broker、Database migration、手機頁 | 本機 broker／server、範例資料與離線畫面 |
| 13-16 | 進度表、故障卡、重建環境 | 備用線材、開發板、電源與展示網路 |
| 17 | 展示順序、電源區、網路與計時 | 延長線、備援網路、影片播放與安全停止檢查 |

## 5. 故障交換順序

不要一看到失敗就換 ESP32。依序留下證據：

1. 檢查電源、GND、極性與電壓。
2. 檢查 GPIO、接線與插接牢固度。
3. 使用最小程式測單一輸入或輸出。
4. 更換一條已知良好線材。
5. 更換同類模組並重測。
6. 最後才更換開發板，並在 `docs/hardware_state.md` 記錄原板現象。

## 6. 跨組與跨電腦紀錄

- 每個實體元件貼唯一標籤，例如 `BOARD-T01`、`SERVO-T02`。
- 借用、接線、供電、library、測試與已知問題記在 `docs/hardware_state.md`。
- 每次完整實驗在 `docs/lab_notes/` 新增日期檔，附 commit 與下一步。
- 開始前 `git pull --ff-only`；完成後只提交教材、程式與去敏紀錄。
- 不提交 Wi-Fi 密碼、API key、Database secrets、學生個資或未授權影像。

## 7. 第 9 週

2026-11-04 不要求到校、不收新的作品或評量證據。選讀 QA 不要求使用
實體器材；需要重新接電、換供電或測試高電流致動器者，延至教師在場時。

## 8. 完成標準

- 材料不足不讓學生整堂等待，另有等價的判斷、設計或 log 除錯任務。
- 不把 teacher-tested、student target-tested 與 physical project-tested 混為一談。
- 每週下課前完成斷電、收納、標籤、版本與 Lab Notebook。
- 題目可自由，但供電、邏輯準位、驅動與安全停止沒有例外。
