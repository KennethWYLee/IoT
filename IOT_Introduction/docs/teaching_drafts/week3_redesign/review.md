# Week 3 重設與檢查紀錄

最新增補：依教師「可加入前幾週教過的零件」要求，已加按鈕＋光敏取樣器與三筆取樣改造題。新版 52 頁，p36 題目／p37 解答；新檢查見 [整合紀錄](../integration_checks/README.md)。以下保留原重設過程與當時頁數，不代表本輪新增已推送或實測。

日期：2026-09-16。用途：教師檢閱完整重設稿，尚未替換正式 notebook／PDF。

## 依據與範圍

教師要求「根據 week2_main 的作法，重新設計 week3_main」。沿用已確認的 Week 2 重設原則：全班跟講義同步操作、先做出可見結果再講原理、明確接點、沒有逐組等待教師批准的課堂流程。

本次先讀原 Week 3 notebook、77 頁 PDF 的基本資訊、Week 2 重設來源及 README、Week 3 分類來源和公開程式、硬體狀態與 2026-08-30／09-05 教材和實測紀錄。課程目錄未找到 PROJECT.md，因此依上述現有來源與教師當次決定，不另創課程事實。

新維護來源為本目錄 `week3_main.md` 與 `build.cjs`；`week3_main.pdf` 為衍生成品。`build_manifest.json` 記錄來源、三份程式、照片與 PDF 的 SHA-256，避免把後續過期輸出當最新版。

原正式 Week 3 notebook／PDF、既有三份 `.ino`、Week 2 主教材與 QA 均未改。沒有發布到 GitHub 或 Google Drive。原有未追蹤的 `examples/course_backend/runtime/iot_course.db` 未處理。

## 新閱讀順序

| 頁碼 | 學生在做什麼 | 段落完成後能解釋什麼 |
|---|---|---|
| 1–3 | 準備器材、斷電拆除 Week 2 按鈕、找實物印字 | 本週腳位改用途，不能沿用舊按鈕接線 |
| 4–7 | 設表筆及直流檔、把 3V3/GND 引出、量一次電壓 | 紅減黑、共同參考、電壓表不等於直接短接 |
| 8–11 | 空板換 GPIO5 程式、再接線量 LOW/HIGH | 程式回報與實際電壓不同；3V3 與 GPIO 不同 |
| 12–16 | 辨認 KY、斷電量三條路徑、供電量 S | 模組兩個電阻及訊號位置，遮光方向依本輪資料 |
| 17–20 | 空板換 ADC 程式、新增 GPIO4 線、各存十筆 | V、raw、lux 不混用；資訊路徑不是接線圖 |
| 21–24 | 算範圍、填分類規則、各五筆新驗證 | 標籤與基準支持程度不同，重疊時不硬分類 |
| 25–29 | 量兩顆電阻、分壓 A/B、做完才算電流 | 中間電壓看比例，總電流不是先被電阻用掉 |
| 30–31 | 紙上練習，下一頁核對解答 | 分界、未判定、電表與安全接法 |
| 32–36 | 保存與收納、需要時查排錯／單位／記憶體／來源 | 留下可追溯紀錄，不以示例代替實測 |
| 37–43 | 三支程式全文 | 與既有公開 `.ino` 一致，不另維護手抄程式 |

## 與原教材的內容對照

- 保留三組 KY 斷電阻值、三種光照操作下的 S 電壓、3V3 一筆與 GPIO LOW/HIGH 各三筆、Reset 的 Serial 啟動紀錄。
- 保留室內與遮光各十筆原始資料、四個基準值、本地相對分類、各五筆獨立驗證；不把舊的不同輪五筆拼成新二十筆。
- 保留固定電阻 1 kΩ／10 kΩ 的獨立阻值、A/B 接線照片、預測、實測與比例解釋；不增購 5 kΩ。
- 原 Discussion 的分壓預測、實際資料比較、情境排查與「大是否更亮」分配到實際操作後，另有整合練習；不增加成績比重。
- 將原前置理論移到觀察後；深層編譯記憶體解讀移到附錄。原案例的大量逐輪數值仍保留在歷史正式教材／lab notes，不帶進主操作線干擾初學者。
- 用「GND／第 3 列、3V3／第 6 列、GPIO5／第 12 列、S／第 15 列」代替反覆使用未先解釋的測點縮寫；TPO 僅作一次舊稿對照。
- 所有新圖依既有實物接法繪製，文字與孔位同頁；圖中顏色不是唯一辨識方式。KY 照片只在排版中局部放大，原始影像不修改。

## 安全與尚待完成

最高優先的課前待辦仍是指定器材的實機確認。BOARD-T01 有歷史 GPIO5 穩態電壓回報；GPIO4 ADC 的候選測試、Upload 與 raw 反應，不等於全班硬體設定已核准。公開例程保留 `-1`，稿中列出的 4／5 不能直接當作對所有板型的操作授權。

教師應在課前完成板型、模組腳位、3V3 供電、GPIO5 輸出、GPIO4 ADC 範圍與本稿接法確認，再公布本班設定。這是備課工作，不設計成上課逐組等教師放行。本稿可先檢閱版面與順序，不標成全班已實測可用。

目前實體 BOARD-T01 歷史最新交接仍是 Week 7 組裝；本次未碰硬體，不能把新圖當成實物已拆線。教材開頭明確要求移除所有電源、記錄舊接法後才拆除外接線。

本稿保留原教學內容範圍，沒有宣稱已通過兩小時課堂計時。實際帶做時間仍需教師試教；不能只因頁數較少就宣稱學生一定能完成。

## 已做檢查

- `node build.cjs`：43 頁；圖片與連結來源可解析、無主內容水平溢出、無頁面內容超框，最後內容距頁尾最小約 45 CSS px。
- `python verify.py`：43 頁 A4；來源雜湊、程式附錄完整性、無殘留 Markdown 粗體符號／模板標記、練習第 30 頁與答案第 31 頁相鄰均通過。
- 以 HTML parser 取出附錄，逐字核對三份既有 `.ino`；除分頁接回的換行外一致。沒有修改程式，所以本次未重新編譯或宣稱新的韌體驗證結果。
- 以獨立數值計算核對 0.3 V、3.0 V、0.3 mA、330 Ω 的 10 mA、門檻 610 與 720；依既有分類程式逐項核對端點、等號和品質原因。這是文件／邏輯核對，不是感測器效能測試。
- 以 `(左或右半部, 列號)` 模型核對共享節點與電阻跨列，確認 a15/c15/e15 相通而 f15 分開、c23/d23/e23 相通。
- 43 頁已全部輸出 PNG，掃描六張全頁聯絡表；放大核對實物腳位、電表接線、KY 放大圖、分壓 A/B、題目／解答與附錄程式。保留重建工具，不把暫存 PNG 納入版本控制。
- 查核 Espressif ADC/GPIO、ESP32-S3 datasheet v2.2、Fluke 電阻／通斷、Analog Devices 分壓文件。Arduino Memory Guide 頁面本次工具未回傳正文，沿用既有來源，不宣稱重新取得全文。

未做：Arduino 編譯、Upload、電表量測、ADC 採樣、學生試教、全班板卡通過確認、commit、push、雲端上傳。

下一步：先以教師實物沿新版接線逐段走一次，保存實際電壓、同輪 raw 和分類限制。完成條件是與圖文一致且安全限制已確認；通過後才考慮取代正式 main 與發布。
